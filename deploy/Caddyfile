{$ADMIN_SITE_ADDRESS} {
	encode gzip zstd

	header {
		Strict-Transport-Security "max-age=31536000; includeSubDomains; preload"
	}

	@health path /healthz
	handle @health {
		respond "ok" 200
	}

	reverse_proxy admin-api:8088
}
